---
-
  hosts: all
  user: vagrant
  sudo: false
  vars:
    env: development
    project_name: {{ cookiecutter.repo_name }}
    ansible_python_interpreter: /usr/local/bin/python
  roles:
    - vagrant
    - common
    - mysql
    - elasticsearch
    - django
